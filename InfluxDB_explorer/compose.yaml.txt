version: '3.8'

services:
  explorer:
    image: influxdata/influxdb3-ui:1.3.0
    container_name: influxdb3-explorer
    ports:
      - "8888:80"
    volumes:
      - influxdb3-explorer-config:/app-root/config:ro
    restart: unless-stopped

volumes:
  influxdb3-explorer-config:
    driver: local
